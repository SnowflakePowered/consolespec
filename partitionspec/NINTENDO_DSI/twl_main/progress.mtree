#mtree
# Nintendo DSi TWL_MAIN:/progress, identical across every firmware
# save files a fresh install leaves blank carry a name only: their
# size and contents change as soon as the title is used
. type=dir
